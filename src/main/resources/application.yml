spring:
  application:
    name: identity-service

  security:
    user:
      name: ${SECURITY_USER}
      password: ${SECURITY_PASSWORD}
    oauth2:
      # OAuth2 Client — для логина через Google, GitHub и т.д.
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET}
            scope:
              - openid
              - email
              - profile
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
      # OAuth2 Authorization Server — твой сервис выдаёт OAuth2 токены другим
      authorizationserver:
        issuer: "${BASE_URL}"


  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  datasource:
    url: jdbc:postgresql://localhost:5432/${POSTGRES_DB}
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

jwt:
  secret: ${JWT_SECRET}
  expiration: ${JWT_EXPIRATION}

frontend:
  url: ${FRONTEND_URL}